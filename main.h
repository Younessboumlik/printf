#ifndef MAIN_H_
#define MAIN_H_

#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

char *int_to_char(int num);
int my_strlen(const char *str);
int _printf(const char *format, ...);

#endif
